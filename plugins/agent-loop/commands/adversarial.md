---
description: Adversarially review current progress using both Claude and Codex
---

Use the `adversarial-review` skill and follow its procedure, discovering
`agent-loop` and running it with engine `claude` as that skill directs, so
the counterpart leg runs in Codex. Report only; do not apply fixes.
Arguments: $ARGUMENTS

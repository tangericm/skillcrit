---
description: Execute the plan unattended until a stop condition is reached
---

Use the `session-state` skill and follow its `/auto` procedure, discovering
`agent-loop` and running it with engine `claude` as that skill directs.
Never merge, never push.

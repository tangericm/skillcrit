---
description: Emit a resume or parallel-work prompt for another Claude or Codex session
---

Use the `session-state` skill and follow its `/handoff` procedure,
discovering `agent-loop` and running it with engine `claude` as that skill
directs. Pass `parallel` through when the user supplied it: $ARGUMENTS

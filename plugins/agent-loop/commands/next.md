---
description: Execute the next concrete step in the plan, then stop
---

Use the `session-state` skill and follow its `/next` procedure, discovering
`agent-loop` and running it with engine `claude` as that skill directs.
Execute exactly one task and stop.

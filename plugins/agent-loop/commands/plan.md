---
description: Create a plan when none exists, so /next and /auto have something to run
---

Use the `session-state` skill and follow its `/plan` procedure, discovering
`agent-loop` and running it with engine `claude` as that skill directs.
Prefer `superpowers:writing-plans` when it is installed. Never create a
second plan when an open one already exists. Goal: $ARGUMENTS

---
description: Report where the current plan stands and what the next concrete step is
---

Use the `session-state` skill and follow its `/status` procedure, discovering
`agent-loop` and running it with engine `claude` as that skill directs. Do
not write any file.
